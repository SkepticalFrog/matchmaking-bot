import { ModalSubmitInteraction } from 'discord.js';

export const lobbyCreation = async (interaction: ModalSubmitInteraction) => {
  console.log('interaction', interaction);
  console.log('OMG cest dans lobby creation !!!')
};
